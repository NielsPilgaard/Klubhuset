Using the file integration for weekplans must be easier to use. Think of similar google, microsoft solutions, and our @docs/PERSONAS.md

- more file types must be allowed (video, audio, images, docs are all valid)
- folder hierachy enabled (create folder, visualize files in folders)
- uploading of large files doesn't work properly (+30MB), we should chunk it and upload via presigned url directly from client for max speed. we also must show progress, so the user doesn't exit or refresh
