# Improvements to make

- on redeploy the application is still briefly down, keycloak shows 404
- we still get logged out after a short while when accessing prod on mobile, usually when we close the browser and open it again after a while
- parent view mode should be implemented
- parent module needs to be gated behind stripe module impl
