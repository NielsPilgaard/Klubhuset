# Improvements to make

1. Seeded tenant admin cannot see parent module, no way to purchase it.
2. The NavBar has many links now, we need to group them by functionality to make it easier to grasp.
3. SFO vagtplan leaves a lot to be desired:
   - We need a print functionality.
   - Make it easy to add the same timeslot for each day (click toggle for example, to make current creation be for all 5 days)
   - Allow adding multiple staff to one SFO block, each school probably has 10-100 children during SFO in the morning or afternoon, 1 adult is not a lot.
   - The SFO schema should be able to specify activities per week, like Schema and Weekplan
