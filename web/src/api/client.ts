/**
 * Typed API client — generated types come from `npm run codegen`.
 * `schema.d.ts` is produced from the OpenAPI spec emitted by the API build.
 *
 * Use createFetch from openapi-fetch to get a fully-typed client once the
 * API has real endpoints and `npm run codegen` has been run.
 *
 * Example (after codegen):
 *   import createClient from 'openapi-fetch'
 *   import type { paths } from './schema'
 *   export const api = createClient<paths>({ baseUrl: '/api/v1' })
 */

export const API_BASE = '/api/v1'
